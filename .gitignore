# Environment
.env
*.env.local

# Python
.venv/
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
.next/

# TypeScript incremental build cache — machine-local derived state,
# rewritten by every `tsc -b` (the typecheck gate). Was tracked until
# 2026-08-22, which left it permanently dirty: end-of-file-fixer adds a
# trailing newline on commit and tsc regenerates the file without one.
*.tsbuildinfo

# IDE
.vscode/
.idea/

# Claude Code
CLAUDE.md
.claude/

# OS
.DS_Store
Thumbs.db

# Docker
*.log
docker-compose.override.yml

# mkdocs build output
site/

backend/static/
frontend/node_modules/

# Design mockups/inspo — local reference only, not part of the repo
/design/
